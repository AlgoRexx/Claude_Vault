{"id": "DG-001", "category": "principles", "rule": "Function over form", "description": "This is a utility tool for a technical user. Visual polish is secondary to clarity and speed. Every UI element must justify its existence by reducing friction or increasing confidence. Decorative elements are not acceptable.", "applies_to": ["all"], "severity": "high"}
{"id": "DG-002", "category": "principles", "rule": "Never hide system state", "description": "The user must always know: what the watcher is doing, what the current session state is, and what the last DB write was. A system that silently succeeds or silently fails is unacceptable. Always surface operational state.", "applies_to": ["all"], "severity": "critical"}
{"id": "DG-003", "category": "principles", "rule": "Confirmation before destruction", "description": "Any action that modifies or removes a file must require explicit user confirmation. This includes archive and delete. A single click is not sufficient for delete — require a secondary confirmation step. Auto-actions that result in data loss are forbidden.", "applies_to": ["cleanup", "file_management"], "severity": "critical"}
{"id": "DG-004", "category": "principles", "rule": "Suggestions are opt-in, uploads are never automatic", "description": "The system may suggest files. It may never upload them without an explicit user action. The distinction between suggestion and action must be visually unambiguous. Auto-upload mode, if implemented, requires an explicit toggle that is off by default and persists its state.", "applies_to": ["suggestion_engine", "upload_flow"], "severity": "critical"}
{"id": "DG-005", "category": "principles", "rule": "Errors are shown, not swallowed", "description": "Every failure — file rejection, hash error, DB write failure, watcher drop, handoff parse error — must surface to the user in a visible, non-dismissible location. A log panel is acceptable. A system tray badge is acceptable. Silent failure is not. Handoff parse errors must appear inline below the paste textarea, naming the exact missing section.", "applies_to": ["all"], "severity": "critical"}
{"id": "DG-006", "category": "color", "rule": "Primary background is dark charcoal, not black", "description": "Background color is #1C1C1C — not #000000. Pure black creates harsh contrast and reads as amateurish in this context. Charcoal reads as technical and refined. Surface/panel background is #252525. Elevated surfaces (modals, drawers) use #2E2E2E.", "applies_to": ["all_ui"], "severity": "critical"}
{"id": "DG-007", "category": "color", "rule": "Coral-red is the single primary accent — used sparingly and with authority", "description": "Primary accent is #FF4B35 (coral red-orange, matching the reference identity). This color is used for: active states, CTAs, session state indicators, key labels, and the brand mark only. It must not be diluted across decorative uses. When coral appears, it must mean something. Secondary text and inactive UI: #9CA3AF. Borders: #333333.", "applies_to": ["all_ui"], "severity": "critical"}
{"id": "DG-008", "category": "color", "rule": "Session state color mapping is non-negotiable", "description": "Session states use the following fixed colors — never repurposed elsewhere: ACTIVE: #9CA3AF (muted gray, neutral). NEAR_LIMIT: #FF4B35 at 70% opacity. FINAL_WINDOW: #FF4B35 at 100% with a 1px animated border pulse. CLOSED: #444444. The NEAR_LIMIT and FINAL_WINDOW states both use the primary coral to create urgency through saturation, not a different hue.", "applies_to": ["session_indicator", "status_bar"], "severity": "critical"}
{"id": "DG-009", "category": "color", "rule": "Confidence scores use opacity of coral, not separate hues", "description": "File suggestion confidence levels are expressed by varying the opacity of #FF4B35: high (100% opacity + label 'HIGH'), medium (55% opacity + label 'MED'), low (25% opacity + label 'LOW'). Do not introduce green or yellow for confidence — the palette stays monochromatic. Color variation signals category. Opacity signals intensity.", "applies_to": ["suggestion_engine_ui"], "severity": "high"}
{"id": "DG-010", "category": "color", "rule": "No gradients. No shadows. No blur effects.", "description": "This design language is flat and hard-edged. No CSS gradients, box-shadows, or backdrop-filter blur on any interactive or structural element. Depth is achieved through color contrast and typographic scale, not layer effects. The only exception: the 1px solid border at #333333 is acceptable for panel separation.", "applies_to": ["all_ui"], "severity": "high"}
{"id": "DG-011", "category": "typography", "rule": "Display typeface is ultra-bold condensed grotesque", "description": "Hero text, section titles, and the application name use a condensed heavy grotesque — Barlow Condensed Black, or Bebas Neue, or a similar ultra-compressed weight. This matches the reference identity's oversized headline language. Fallback: system-ui with font-stretch: condensed and font-weight: 900.", "applies_to": ["display_text", "app_title", "section_headers"], "severity": "critical"}
{"id": "DG-012", "category": "typography", "rule": "UI body copy uses a clean geometric sans-serif — not Inter", "description": "Navigation, labels, and body text use DM Sans, Neue Haas Grotesk, or Aktiv Grotesk. Inter and Roboto are explicitly excluded — they read as generic. Font weight for UI labels: 400. For active/selected states: 600. Never 700+ for body copy.", "applies_to": ["navigation", "labels", "body_text"], "severity": "high"}
{"id": "DG-013", "category": "typography", "rule": "Monospace is mandatory for all system data", "description": "File names, file paths, hashes, session IDs, account IDs, file IDs, timestamps, addresses, DB values, limit countdowns, and event log entries must render in monospace. Use JetBrains Mono, IBM Plex Mono, or Fira Code. Monospace must be uppercase for metadata (timestamp, state labels). Mixed case for file names and paths. The handoff template output block and paste textarea in the HANDOFF tab also render in monospace — they are system data, not prose.", "applies_to": ["file_list", "event_log", "db_view", "metadata", "accts_tab", "handoff_tab"], "severity": "critical"}
{"id": "DG-014", "category": "typography", "rule": "Type scale has exactly four levels", "description": "Level 1 (hero/app name): condensed grotesque, 48–96px, weight 900, uppercase. Level 2 (section header): condensed grotesque, 24px, weight 700, uppercase. Level 3 (UI body): geometric sans, 13px, weight 400. Level 4 (metadata): monospace, 11px, weight 400, uppercase, letter-spacing 0.08em. Do not introduce intermediate sizes.", "applies_to": ["all_ui"], "severity": "high"}
{"id": "DG-015", "category": "typography", "rule": "Superscript numerals on category labels", "description": "When a navigation section contains subcategories, append a superscript count in the accent color: Sessions⁴, Projects². This matches the reference navigation language precisely. Do not use badges, pills, or parenthetical counts — only HTML sup elements in #FF4B35.", "applies_to": ["navigation", "category_labels"], "severity": "medium"}
{"id": "DG-016", "category": "typography", "rule": "Subcategory items use the arrow-indent prefix", "description": "Nested navigation items and file subcategories must use the '↳' arrow prefix (U+21B3) in #FF4B35, followed by a non-breaking space before the label. This is a visual convention lifted directly from the reference identity and must remain consistent. No bullets, no dashes, no chevrons.", "applies_to": ["navigation_subitems", "file_subcategories"], "severity": "high"}
{"id": "DG-017", "category": "typography", "rule": "Truncate long paths from the left, not the right", "description": "File paths that exceed column width must truncate from the left (show the filename end). Example: '...sessions/abc/files/model_results.csv' not 'C:/Users/user/Dow...'. The filename itself must always be visible. Implement using CSS direction: rtl on a container with overflow: hidden and a ltr inner span.", "applies_to": ["file_list", "path_display"], "severity": "high"}
{"id": "DG-018", "category": "visual_identity", "rule": "Pixel-grid elements for iconography", "description": "Icons, logo marks, and decorative glyphs use a pixel/bitmap grid aesthetic — consistent with the reference UX mark rendered in 8px-aligned squares. No smooth SVG paths for identity elements. System icons (arrows, chevrons) use geometric, squared-off forms. Rounded corners are not used anywhere in the icon system.", "applies_to": ["icons", "logo", "ui_marks"], "severity": "high"}
{"id": "DG-019", "category": "visual_identity", "rule": "Oversized typography as the primary visual element", "description": "The application name and primary section headers should be set at an aggressively large scale — large enough that text bleeds off the container edge at some viewport sizes. This is a deliberate aesthetic choice, not overflow. Clip with overflow: hidden on the parent. This creates the editorial tension present in the SHIFT5 reference.", "applies_to": ["hero_sections", "app_title", "section_dividers"], "severity": "medium"}
{"id": "DG-020", "category": "visual_identity", "rule": "Brand mark uses a diamond/chevron pixel glyph", "description": "The application logo mark is a pixelated diamond or double-chevron in coral. It must be constructed from geometric shapes on a grid, not a typeface character. It appears only in #FF4B35. Minimum size: 20x20px. It is not used as a loading indicator or decorative repeat.", "applies_to": ["logo", "brand_mark"], "severity": "medium"}
{"id": "DG-021", "category": "layout", "rule": "Four panel model", "description": "The primary UI is organized into four regions: (1) Top bar — system status and active session indicator. (2) Left panel — project and session navigator. (3) Main panel — file list for selected session or project. (4) Bottom bar — watcher status, last event, DB path. No fifth region is added without explicit product approval.", "applies_to": ["electron_ui"], "severity": "high"}
{"id": "DG-022", "category": "layout", "rule": "No modal overload", "description": "Modals are permitted only for: delete confirmation, archive confirmation, manual session reassignment. All other interactions happen inline. Suggestion panels are inline drawers, not modals. The HANDOFF tab is a full tab panel, not a modal.", "applies_to": ["all_ui"], "severity": "medium"}
{"id": "DG-023", "category": "layout", "rule": "Density-first layout with 32px row height", "description": "Default to compact row height (32px per file row). Do not use card layouts for file lists. Cards are acceptable only for project-level views. Row hover state: background #252525. Selected state: 1px left border in #FF4B35 + background #252525.", "applies_to": ["file_list", "session_view"], "severity": "high"}
{"id": "DG-024", "category": "layout", "rule": "Navigation mirrors the reference: flat, multi-column, no sidebar rail", "description": "Top navigation is structured in columns, not a hamburger menu or collapsible sidebar. Primary items are full weight in coral. Sub-items use the ↳ prefix. Navigation text is in #FF4B35. Background is #1C1C1C. No hover animations — only immediate state change on interaction.", "applies_to": ["navigation"], "severity": "high"}
{"id": "DG-025", "category": "interaction", "rule": "Watcher status is always visible in the bottom bar", "description": "The file watcher status (WATCHING / PAUSED / ERROR) must be visible in the bottom status bar in Level 4 monospace, uppercase. Status string format: 'WATCHER · {STATE} · {LAST_EVENT_TIMESTAMP}'. If the watcher drops, the STATE changes to ERROR and text renders in #FF4B35 immediately.", "applies_to": ["status_bar", "watcher_ui"], "severity": "critical"}
{"id": "DG-026", "category": "interaction", "rule": "File suggestion panel renders before the user types", "description": "When a new session is created, the suggestion panel is shown immediately — not after the first message, not after a click. The user must see relevant file suggestions before they start typing. This is the primary value delivery moment of the whole system.", "applies_to": ["suggestion_engine_ui", "session_start_flow"], "severity": "high"}
{"id": "DG-027", "category": "interaction", "rule": "Pixel-grid checkboxes — no border-radius", "description": "Checkboxes in the suggestion panel are 14x14px, 1px border in #FF4B35, checked state fills with #FF4B35, no border-radius. This is consistent with DG-018 pixel-grid identity. No system-default checkboxes — always custom rendered.", "applies_to": ["suggestion_engine_ui", "forms"], "severity": "medium"}
{"id": "DG-028", "category": "interaction", "rule": "Delete confirmation requires typing the project name", "description": "If the user selects 'Delete all' for a project, they must type the project name into an input field before the action proceeds. The input border turns #FF4B35 when the typed value matches. The confirm button remains disabled until match. Archive does not require this — it is reversible.", "applies_to": ["cleanup_ui"], "severity": "critical"}
{"id": "DG-029", "category": "interaction", "rule": "Cleanup prompt shows size, count, and archive path", "description": "When presenting the cleanup prompt for a project, always display: total file count, total size on disk (human-readable), and the full archive destination path in monospace. Action buttons: [ARCHIVE] (default, coral) and [DELETE] (requires secondary confirmation) and [KEEP].", "applies_to": ["cleanup_ui"], "severity": "high"}
{"id": "DG-030", "category": "logging", "rule": "Event log is append-only and monospace-rendered", "description": "The file_events table is the source of truth for all system activity. Log entries must never be deleted, updated, or overwritten. The log UI renders in Level 4 monospace (11px, uppercase, #9CA3AF) with event type in #FF4B35. Format: '{TIMESTAMP} · {EVENT_TYPE} · {FILE_NAME} · {DETAIL}'.", "applies_to": ["database", "event_log"], "severity": "critical"}
{"id": "DG-031", "category": "logging", "rule": "All rejected files are logged with reason codes", "description": "Rejection reason codes: FILE_DISAPPEARED, SIZE_ZERO, EXTENSION_BLOCKED, DUPLICATE_HASH, PARTIAL_DOWNLOAD, VALIDATION_FAILED. Rejected events render in #FF4B35 at 60% opacity in the event log UI to distinguish from destructive events at 100%.", "applies_to": ["ingestion_pipeline", "event_log_ui"], "severity": "high"}
{"id": "DG-032", "category": "cli", "rule": "CLI is the primary interface for v1", "description": "Before any Electron or web UI is built, the system must be fully operable via CLI. Output labels are uppercase, values are mixed case, state strings use ANSI color codes matching the coral accent where the terminal supports it.", "applies_to": ["cli"], "severity": "high"}
{"id": "DG-033", "category": "configuration", "rule": "All tuneable values are in a single config.json, not hardcoded", "description": "Externalize to config.json: watch directory, file type allowlist, stabilization threshold, NEAR_LIMIT and FINAL_WINDOW thresholds, cleanup TTL in days, archive directory, DB path, max_concurrent_sessions, and limit_windows (5HR / DAILY / WEEKLY in milliseconds). Hardcoding any of these is not acceptable. No API keys belong in config.json — this tool makes no outbound network calls.", "applies_to": ["all"], "severity": "high"}
{"id": "DG-034", "category": "configuration", "rule": "config.json is validated at startup with explicit error messages", "description": "At startup, validate config.json against a schema. Missing keys, wrong types, or invalid paths must produce a specific error message and halt startup. Do not silently fall back to defaults for required fields. Error format: 'CONFIG ERROR · {KEY} · {REASON}'.", "applies_to": ["startup", "configuration"], "severity": "high"}
{"id": "DG-035", "category": "macos_widget", "rule": "ClaudeVault lives in the macOS menu bar — not as a window", "description": "ClaudeVault's primary UI surface is a macOS menu bar widget (NSStatusItem). It does not open a standalone window. All interactions happen inside the popover anchored below the menu bar icon. The Electron main window is hidden at launch. The only entry point is the menu bar icon.", "applies_to": ["macos_widget", "app_shell"], "severity": "critical"}
{"id": "DG-036", "category": "macos_widget", "rule": "Menu bar icon is a 5×5 pixel-grid mark at 33×33px rendered weight", "description": "The menu bar icon is a 5×5 CSS grid of 5px cells with 2px gaps. Total rendered size: 33px (5×5px cells + 4×2px gaps). This size matches the visual weight of macOS system icons (WiFi, Bluetooth, battery) in the menu bar. Only cells at positions [0,2], [1,1], [1,3], [2,0], [2,4], [3,1], [3,3], [4,2] are filled with #FF4B35 — forming an X/diamond pattern. All other cells are transparent. No text, no border, no background on the icon itself. The icon sits inside a cv-trigger container of width:28px height:24px with display:flex align-items:center justify-content:center.", "applies_to": ["menu_bar_icon"], "severity": "critical"}
{"id": "DG-037", "category": "macos_widget", "rule": "A 6px coral square badge on the icon signals pending action", "description": "When ClaudeVault has a pending action (cleanup due, unlinked files, new session ready), a 6×6px coral (#FF4B35) square badge appears at top-right of the menu bar icon, absolutely positioned at top:3px right:3px. The badge has no border-radius — it is a hard square, consistent with the pixel-grid identity. It pulses (opacity 1 → 0.3 → 1) at 1.2s interval when the session state is NEAR_LIMIT or higher.", "applies_to": ["menu_bar_icon", "badge"], "severity": "high"}
{"id": "DG-038", "category": "macos_widget", "rule": "The popover is exactly 320px wide and anchors top-right below the icon", "description": "The popover panel is fixed at width: 320px. It opens directly below the menu bar, flush with the right edge of the icon. Top corners are square (border-radius: 0 on top edges). Bottom corners use border-radius: 8px. Border: 1px solid #333333. The popover does not reposition or resize at any viewport size. If the content inside a tab panel exceeds available height, that panel scrolls internally — the popover height does not grow.", "applies_to": ["popover"], "severity": "critical"}
{"id": "DG-039", "category": "macos_widget", "rule": "Popover background is #111111 — darker than the app charcoal", "description": "The popover uses #111111 (near-black) as its base background, not #1C1C1C. This creates separation from the macOS desktop behind it. Panels inside the popover use #1C1C1C for surface and #252525 for elevated rows. The header and tab bar use #111111 to maintain a unified dark band at the top.", "applies_to": ["popover", "popover_header", "tab_bar"], "severity": "high"}
{"id": "DG-040", "category": "macos_widget", "rule": "Popover header: pixel mark + CLAUDEVAULT wordmark + state indicator in one row", "description": "The popover header is a single row, height: auto, padding: 10px 14px 8px. Left side: 5×5 pixel mark (4px cells, 1.5px gaps = 23px) + CLAUDEVAULT in Barlow Condensed Black 18px uppercase coral. Right side: state indicator = 5px square dot (color-coded per DG-008) + state label in IBM Plex Mono 9px uppercase. The header background is #111111. Border-bottom: 1px solid #333333. No other elements in the header.", "applies_to": ["popover_header"], "severity": "critical"}
{"id": "DG-041", "category": "macos_widget", "rule": "Cleanup banner sits between header and tabs — always visible regardless of active tab", "description": "When a cleanup-eligible project exists, a banner renders below the header and above the tab bar. It is not inside any tab panel. It remains visible on all four top-level tabs. Background: rgba(255,75,53,0.07). Border-bottom: 1px solid rgba(255,75,53,0.2). Layout: warning triangle glyph (⚠) + monospace text 'CLEANUP: {PROJECT} · {N} FILES · {SIZE}' on the left, REVIEW button on the right. If no cleanup is pending, this banner is absent — do not render an empty container.", "applies_to": ["cleanup_banner"], "severity": "critical"}
{"id": "DG-042", "category": "macos_widget", "rule": "REVIEW button in the cleanup banner uses border-radius — the only rounded button in the system", "description": "The REVIEW button is an intentional exception to the pixel-grid, sharp-edge rule. It uses border-radius: 6px, border: 1px solid #FF4B35, background: transparent, color: #FF4B35, font: IBM Plex Mono 10px uppercase. The rounding distinguishes it as a CTA inside the banner's warning context. All other buttons in ClaudeVault are sharp-edged (border-radius: 0). The COPY TEMPLATE button in the HANDOFF tab is a second intentional exception — see DG-067.", "applies_to": ["cleanup_banner", "review_button"], "severity": "medium"}
{"id": "DG-043", "category": "macos_widget", "rule": "Tab bar has exactly four top-level tabs in fixed order", "description": "The tab bar renders four top-level tabs in this exact order: SESSION | FILES | TRACK | OPS. TRACK contains ACCTS and CHATS as sub-tabs. OPS contains ACTIONS and HANDOFF sections in one scrollable panel. 'FILES' remains unified (suggested + recent). Each top-level tab is flex:1, padding: 6px 0, text-align: center. Font: IBM Plex Mono 8px uppercase letter-spacing 0.06em. Inactive: color #555555. Hover: color #9CA3AF. Active: color #FF4B35 + border-bottom: 2px solid #FF4B35. Background of the tab bar: #111111. No background highlight on active tab — the underline alone marks active state.", "applies_to": ["tab_bar"], "severity": "critical"}
{"id": "DG-044", "category": "macos_widget", "rule": "SESSION tab: two key-value blocks separated by a divider", "description": "The SESSION panel has two blocks, each with padding: 12px 14px and border-bottom: 1px solid #333333. Block 1 (session data): SESSION ID, PROJECT, STARTED, STATE, FILES LINKED, MESSAGES — each as a row. Block 2 (watcher data): WATCHER, WATCH DIR, LAST DETECT, DB SIZE. Every row is display:flex justify-content:space-between. Key: IBM Plex Mono 9px uppercase #555555. Value: IBM Plex Mono 10px #F0EDE8. Values that are coral: SESSION ID value, STATE value, MESSAGES value when near or at limit. Below the MESSAGES row, a 3px progress bar fills the full block width.", "applies_to": ["session_tab"], "severity": "critical"}
{"id": "DG-045", "category": "macos_widget", "rule": "Progress bar is 3px tall, coral fill, anchored below the MESSAGES row", "description": "The session message progress bar: height 3px, width 100% of the containing block (minus padding). Track background: #333333. Fill: rgba(255,75,53,0.7). Fill width is calculated as (current_messages / limit_messages) × 100%. The bar has no border-radius. It renders flush to the bottom of the key-value block, with margin-top: 8px from the last row. No percentage label — the MESSAGES row value already shows the count.", "applies_to": ["progress_bar", "session_tab"], "severity": "high"}
{"id": "DG-046", "category": "macos_widget", "rule": "WATCHER state uses #4ADE80 green — the only semantic green in the system", "description": "The WATCHER value in the SESSION tab and the status bar renders in #4ADE80 (green) when state is WATCHING. This is the only use of green in the entire UI. It is a semantic exception — green = running process, not a design accent. ERROR renders in #FF4B35 coral. PAUSED renders in #9CA3AF gray. Never use green for any other purpose.", "applies_to": ["session_tab", "status_bar", "watcher_state"], "severity": "high"}
{"id": "DG-047", "category": "macos_widget", "rule": "FILES tab: rows are 32px tall, left-border on most-recent file, right-aligned type badge", "description": "Each file row in the FILES tab is display:grid grid-template-columns:1fr 52px, height:32px, padding:0 14px. The filename column uses direction:rtl with an inner ltr span for left-truncation. The right column stacks timestamp (top, IBM Plex Mono 9px #555555) and file type badge (bottom, IBM Plex Mono 8px #FF4B35 at 70% opacity, uppercase). The most recently ingested file row has border-left: 2px solid #FF4B35 and padding-left: 12px (2px less to compensate). All other rows: no left border, padding-left: 14px.", "applies_to": ["files_tab"], "severity": "high"}
{"id": "DG-048", "category": "macos_widget", "rule": "SUGGEST tab: header with title + Upload Selected button, then suggestion rows", "description": "The SUGGEST panel header is a distinct row: background #111111, padding 8px 14px, border-bottom 1px solid #333333. Left: 'New Session · Suggested' in IBM Plex Mono 9px uppercase coral with a superscript count. Right: 'Upload Selected' button (sharp-edged, 1px solid #FF4B35, coral text). Suggestion rows are display:flex align-items:center gap:10px, height 36px, padding 7px 14px. Layout per row: [12px pixel-checkbox] [file info block flex:1] [confidence badge]. Clicking a row toggles the checkbox.", "applies_to": ["suggest_tab"], "severity": "critical"}
{"id": "DG-049", "category": "macos_widget", "rule": "LOG tab: three-column fixed grid per entry", "description": "Each log entry is display:grid grid-template-columns:60px 72px 1fr, padding:5px 14px, border-bottom:1px solid rgba(51,51,51,0.4). Column 1 (timestamp): IBM Plex Mono 9px #555555. Column 2 (event type): IBM Plex Mono 9px uppercase, color varies by type — CREATED:#FF4B35, LINKED:rgba(255,75,53,0.75), REJECTED:rgba(255,75,53,0.4), DEDUPLICATED:rgba(255,75,53,0.35), SESSION START:rgba(255,75,53,0.6). Column 3 (detail): IBM Plex Mono 9px #9CA3AF, truncates with overflow:hidden. No icons, no indentation, no grouping headers.", "applies_to": ["log_tab"], "severity": "high"}
{"id": "DG-050", "category": "macos_widget", "rule": "OPS tab: ACTIONS section followed by HANDOFF section", "description": "The OPS panel contains both ACTIONS and HANDOFF in one scrollable view. ACTIONS section: state-setting buttons stacked vertically and quick-action grid 2 columns. HANDOFF section: same two-step flow and parser behavior as DG-066 and DG-067. This merge must not remove any actions or handoff functionality.", "applies_to": ["ops_tab"], "severity": "high"}
{"id": "DG-051", "category": "macos_widget", "rule": "Persistent bottom status bar: 3 items, always visible, never inside a tab", "description": "The bottom status bar sits below all tab panels and is always visible. Background: #111111. Border-top: 1px solid #333333. Padding: 5px 14px. Three items in display:flex justify-content:space-between. Item 1: 4px coral pulsing square + 'WATCHING' in IBM Plex Mono 8px coral (or gray if PAUSED, coral if ERROR). Item 2: 'LAST · {TIMESTAMP}' in IBM Plex Mono 8px #555555. Item 3: 'DB · {SIZE}' in IBM Plex Mono 8px #555555. The dot pulses continuously when WATCHING. No separator characters between items — space-between handles layout.", "applies_to": ["status_bar"], "severity": "critical"}
{"id": "DG-052", "category": "macos_widget", "rule": "Desktop background behind the popover is deep teal-black, not charcoal", "description": "The macOS desktop shown behind the ClaudeVault popover uses #0A1A17 (deep teal-black) as the wallpaper color. This is not a configurable UI surface — it is the system wallpaper. The popover's #111111 background creates visible contrast against it, making the panel edges legible without a drop shadow. No drop shadow is added to the popover — contrast alone provides separation.", "applies_to": ["desktop", "popover_context"], "severity": "medium"}
{"id": "DG-053", "category": "macos_widget", "rule": "Menu bar system icons use white at 50–60% opacity — ClaudeVault icon is the exception at full coral", "description": "In the macOS menu bar, system-provided icons (WiFi, battery, clock, spotlight) render at white with 50–60% opacity to sit quietly in the bar. The ClaudeVault pixel mark is the only icon in the bar that uses a named color (#FF4B35 coral) at full opacity. This intentional contrast makes the ClaudeVault icon immediately findable without searching. The trigger container highlights to rgba(255,75,53,0.2) background when the popover is open.", "applies_to": ["menu_bar_icon", "menu_bar_context"], "severity": "high"}
{"id": "DG-054", "category": "macos_widget", "rule": "Section title labels inside panels are IBM Plex Mono 9px uppercase #555555 with 10px bottom margin", "description": "Inside SESSION, FILES, TRACK (ACCTS/CHATS), OPS (ACTIONS/HANDOFF), and any future panel that needs a section label, section titles render in IBM Plex Mono 9px uppercase letter-spacing:0.1em color:#555555, with margin-bottom:8px. They are not bold, not coral, not bordered. They are purely organizational labels that fade into the background.", "applies_to": ["panel_section_labels"], "severity": "medium"}
{"id": "DG-055", "category": "macos_widget", "rule": "Row hover state is background #252525 — no transition delay", "description": "All interactive rows (file rows, suggestion rows, log rows, action buttons, account rows) use background:#252525 on hover. The transition is immediate (transition:background 0.08s) — not 0.2s or 0.3s. Fast feedback signals a utility tool, not a consumer app. The selected/active row uses background:#252525 permanently plus the 2px left coral border. There is no separate 'focus' ring on rows — the border-left is the focus indicator.", "applies_to": ["all_interactive_rows"], "severity": "medium"}
{"id": "DG-056", "category": "session_display", "rule": "Session display format is always HASH ~ alias — never alias alone", "description": "Every place a session is referenced in the UI, CLI, or logs must use the format: '{session_id} ~ {alias}' when an alias exists, or '{session_id}' alone when no alias is set. Examples: 'TRD-A1B2 ~ trading-dashboard' or 'TRD-9F1C'. The hash always leads. The separator is exactly ' ~ ' (space, tilde, space). The alias never appears without its hash. Never abbreviate the hash. This rule applies in: popover header, session switcher, history list, file rows, event log, and CLI output.", "applies_to": ["all_session_references"], "severity": "critical"}
{"id": "DG-057", "category": "session_display", "rule": "Alias is rendered in a visually subordinate style to the hash", "description": "In the session display format 'HASH ~ alias', the hash is rendered in #FF4B35 (coral) at full opacity. The ' ~ ' separator and the alias text are rendered in #9CA3AF (secondary gray) at the same font size. This makes the hash the primary identifier and the alias a human-readable annotation — never the reverse. Font: IBM Plex Mono throughout.", "applies_to": ["session_switcher", "popover_header", "history_list"], "severity": "high"}
{"id": "DG-058", "category": "session_display", "rule": "Inline rename — double-click on alias to edit, no modal", "description": "Renaming a session alias is triggered by double-clicking the alias portion of the session display string, or by a pencil icon (✎) that appears on hover next to the alias. Clicking the pencil or double-clicking replaces the alias text with an inline input field, pre-filled with the current alias (or empty if none). Font and size are identical to the display text — the input should feel like editing in place. Pressing Enter or clicking away commits the change. Pressing Escape cancels. The hash portion never becomes editable. Validation: [a-zA-Z0-9_-], max 32 chars. Invalid characters are rejected silently — the character is simply not typed.", "applies_to": ["session_rename_ui"], "severity": "high"}
{"id": "DG-059", "category": "macos_widget", "rule": "SESSION tab is split into three sections: Active Sessions, Focused Session Detail, History", "description": "The SESSION tab has three vertically stacked sections. Section 1 (Active Sessions): a list of all ACTIVE sessions. Each row shows 'HASH ~ alias' (or just hash), project name, and a focus indicator. The currently focused session has a 2px left coral border. Clicking any row switches focus to that session. Section 2 (Focused Session Detail): the key-value block for the currently focused session (SESSION ID, PROJECT, STARTED, STATE, FILES LINKED, MESSAGES + progress bar, WATCHER, WATCH DIR, LAST DETECT, DB SIZE). Section 3 (History): closed sessions. See DG-061.", "applies_to": ["session_tab"], "severity": "critical"}
{"id": "DG-060", "category": "macos_widget", "rule": "Active session switcher rows are 36px tall with focus indicator and hover state", "description": "Each active session row in the switcher is height:36px, padding:0 14px, display:flex align-items:center justify-content:space-between. Left side: 'HASH ~ alias' in IBM Plex Mono 10px (hash in coral, separator+alias in #9CA3AF). Right side: project name in IBM Plex Mono 9px #555555 plus compact icon buttons: DELETE (warn) and REVEAL (folder/arrow). The focused session row has border-left:2px solid #FF4B35 and padding-left:12px. Non-focused rows have no left border. Hover: background #252525. When focused, also show the 'FOCUSED' label in IBM Plex Mono 8px coral on the far right.", "applies_to": ["session_switcher_rows"], "severity": "high"}
{"id": "DG-061", "category": "macos_widget", "rule": "History section shows CLOSED sessions with reopen/delete/reveal actions — sorted most-recently-closed first", "description": "The HISTORY section header label reads 'Session History' in IBM Plex Mono 9px uppercase #555555. Each history row is height:40px, padding:0 14px, display:grid grid-template-columns:1fr 48px. Left column (stacked): line 1 — 'HASH ~ alias' in IBM Plex Mono 10px (hash coral, separator+alias #9CA3AF). Line 2 — 'project-name · {file_count} files · closed {relative_time}' in IBM Plex Mono 9px #555555. Right column: compact icon buttons side-by-side: REOPEN, DELETE (warn), and REVEAL (folder/arrow). Clicking REOPEN calls reopenSession() and moves the session back into the Active Sessions section. Clicking DELETE triggers permanent deletion with an explicit confirmation dialog.", "applies_to": ["session_history_rows"], "severity": "critical"}
{"id": "DG-062", "category": "macos_widget", "rule": "Popover header shows focused session with alias — not a generic app title row", "description": "The popover header dynamicially replaces the static 'CLAUDEVAULT' wordmark with the active session name (alias or project). Right side of the header becomes: 'HASH · STATE' in IBM Plex Mono 10px #555555 + state indicator dot. When no session is focused (app just launched), the left side shows 'CLAUDEVAULT' and the right side shows 'NO ACTIVE SESSION'.", "applies_to": ["popover_header"], "severity": "high"}
{"id": "DG-063", "category": "session_display", "rule": "Max concurrent sessions is 5 — at limit, show a warning in the switcher", "description": "When the number of ACTIVE sessions reaches the configured max (default 5), a warning banner appears at the top of the Active Sessions section: '⚠ MAX SESSIONS REACHED · 5/5' in IBM Plex Mono 9px uppercase rgba(255,75,53,0.7). The 'New Session' button in the ACTIONS tab is disabled (opacity:0.3, cursor:not-allowed) until a session is closed. Reopening a closed session from History is also blocked with the same message.", "applies_to": ["session_switcher", "actions_tab"], "severity": "high"}
{"id": "DG-064", "category": "session_display", "rule": "Session DELETE is permanent and cannot be undone", "description": "Active and history session rows include a compact DELETE icon button. Clicking it triggers an explicit confirmation dialog that warns the action cannot be undone. On confirmation, all database records for the session (session row + focus history + linked file associations) are removed, and all files within the session directory under ~/Downloads/claude-vault/projects/<project_id>/sessions/<session_id>/ are permanently deleted from disk. If the parent project directory becomes empty after session deletion, it is deleted as well.", "applies_to": ["session_tab", "session_management"], "severity": "critical"}
{"id": "DG-065", "category": "account_display", "rule": "ACCTS undo X revokes mistaken limit HIT only", "description": "In the ACCTS tab, each expanded limit row (5HR/DAILY/WEEKLY) shows an icon `X` undo button only when that limit is currently in HIT state (reset_at > now()). The button must be hidden when the row is `NOT LOGGED` or `OK`. Clicking the `X` revokes the most recent active limit hit for that (account_id, limit_type) by removing the latest matching `account_limit_events` row from SQLite, then refreshes the UI so the status/counter updates immediately. If there is no active hit to undo, the IPC must throw and the UI must not change state.", "applies_to": ["accts_tab", "account_limit_rows"], "severity": "critical"}
{"id": "DG-066", "category": "macos_widget", "rule": "HANDOFF tab: two-step layout — STEP 1 copy prompt, STEP 2 paste and parse", "description": "The HANDOFF tab renders two sequential steps vertically. STEP 1 header: section label 'STEP 1 — COPY THIS PROMPT INTO CLAUDE' in IBM Plex Mono 9px uppercase #555555, margin-bottom:8px. Below it: a read-only monospace block (background:#0C0C0C, border:1px solid #2a2a2a, padding:8px 10px, IBM Plex Mono 8px #9CA3AF, max-height:80px, overflow-y:auto) showing the 6-section summary prompt verbatim. Immediately below: a full-width 'COPY PROMPT' button (sharp-edged, border:1px solid rgba(255,75,53,0.35), color:rgba(255,75,53,0.6), IBM Plex Mono 9px uppercase). A divider (1px solid #222, margin:12px 0) separates the steps. STEP 2 header: section label 'STEP 2 — PASTE CLAUDE'S RESPONSE, GET TEMPLATE' in IBM Plex Mono 9px uppercase #555555, margin-bottom:8px. Below it: an editable textarea (background:#0C0C0C, border:1px solid #2a2a2a, color:#9CA3AF on empty / #F0EDE8 when filled, IBM Plex Mono 8.5px, padding:8px 10px, height:88px, resize:none, focus border:rgba(255,75,53,0.35)) with placeholder 'Paste Claude's 6-section summary here...'. Below the textarea: a full-width 'PARSE → BUILD TEMPLATE' button (border:1px solid rgba(255,75,53,0.5), color:#FF4B35, IBM Plex Mono 9px uppercase). PARSE button is disabled (opacity:0.3, cursor:not-allowed) when the textarea is empty.", "applies_to": ["handoff_tab"], "severity": "critical"}
{"id": "DG-067", "category": "macos_widget", "rule": "HANDOFF tab: template output block and COPY TEMPLATE button appear after successful parse", "description": "After a successful parse, the template output block renders below the PARSE button without a page reload. Output block: background:#0C0C0C, border:1px solid #2a2a2a, padding:10px, IBM Plex Mono 8.5px, max-height:220px, overflow-y:auto. Section headers (OBJECTIVE:, CURRENT STATE:, KEY DECISIONS:, IMPORTANT CONTEXT:, FILES:, NEXT STEPS:) render in #FF4B35. Body text renders in #F0EDE8. The closing two lines ('Continue exactly...' / 'Do not restart...') render in rgba(255,75,53,0.5). Below the output block: a full-width 'COPY TEMPLATE' button with border-radius:6px — a second intentional exception to the sharp-edge rule alongside the REVIEW button (DG-042), because both are the terminal CTA of their respective flows. Border: 1px solid #4ADE80. Color: #4ADE80. IBM Plex Mono 9px uppercase. After click: label changes to 'COPIED ✓' for 2.5 seconds then reverts. If a parse error occurs, the template output block does not render. Instead, an inline error appears below the PARSE button: IBM Plex Mono 8px coral, format 'PARSE ERROR · MISSING SECTION: {LABEL}'. The textarea border turns rgba(255,75,53,0.4). The textarea remains editable for correction and re-submission.", "applies_to": ["handoff_tab"], "severity": "critical"}
{"id": "DG-068", "category": "FILESYSTEM", "rule": "Eager Directory Creation", "description": "Session directories (~/Downloads/claude-vault/projects/<project_id>/sessions/<session_id>) must be created immediately upon session creation (start-session) and verified upon reopening (reopen-session) to ensure 'Reveal in Finder' and other file-system operations are always available even before files are uploaded.", "applies_to": ["session_manager"], "severity": "important"}
{"id": "DG-069", "category": "macos_widget", "rule": "CHATS tab history: chat name + account highlight in Level 3, followed by metadata in Level 4", "description": "Each chat history row in the CHATS tab (TRACK panel) displays the Chat Name in DM Sans 13px 600 weight #F0EDE8. The associated Account Alias is displayed on the same line, prefixed by a dot (·) and rendered in DM Sans 13px 900 weight #FF4B35 uppercase (class .chat-account-highlight). Below this line: timestamp (IBM Plex Mono 8px #555555) and session tag (IBM Plex Mono 7px #FF4B35 with 1px border). The latest entry is marked with border-left: 2px solid #FF4B35 and a 'LATEST' badge. Optional notes use the ↳ prefix in IBM Plex Mono 9px #9CA3AF. Rows are background:rgba(255,255,255,0.02) with border:1px solid rgba(255,255,255,0.05). Hover state: background:rgba(255,75,53,0.1).", "applies_to": ["chats_tab", "history_rows"], "severity": "high"}